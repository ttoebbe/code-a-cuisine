import { Component } from '@angular/core';

/**
 * Library route placeholder. Full cookbook UI ships in a later phase.
 */
@Component({
  selector: 'app-library',
  imports: [],
  templateUrl: './library.html',
  styleUrl: './library.scss',
})
export class Library {}
